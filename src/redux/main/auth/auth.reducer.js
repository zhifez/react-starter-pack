import * as actionTypes from '../../actionTypes';

const initialState = {
  loadData: null
};

const reducer = ( state = initialState, action ) => {
  switch ( action.type ) {
  default:
    return state;
  }
}

export default reducer;
